# Contract test evidence

Automated runtime tests cover installation verification, Non-SNAP signing,
hosted and selected-channel checkout, status normalization, catalog mappings,
and webhook signature rejection. Real DOKU sandbox certification still requires
a merchant Client ID, Secret Key, and notification URL configured in Back Office.
