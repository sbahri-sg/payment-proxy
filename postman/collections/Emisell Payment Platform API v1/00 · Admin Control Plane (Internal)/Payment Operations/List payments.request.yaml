$kind: http-request
description: List canonical payments with environment, provider, status, search, and pagination query filters.
url: "{{base_url}}/api/v1/payment-sessions?environment={{execution_mode}}&provider=xendit&limit=25&offset=0"
method: GET
queryParams:
  environment: "{{execution_mode}}"
  provider: xendit
  limit: "25"
  offset: "0"
examples: ./.resources/List payments.resources/examples
order: 1000
