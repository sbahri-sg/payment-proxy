$kind: http-request
description: Menjalankan verifikasi backend otomatis terhadap artifact, exact shared runtime, credential schema, test profile, dan canonical method mapping. Dashboard menampilkan hasil internal CERTIFIED sebagai VERIFIED.
url: "{{base_url}}/api/v1/admin/provider-apps/{{provider_app_id}}/transition"
method: POST
headers:
  X-Admin-API-Key: "{{admin_api_key}}"
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "expected_status": "VALIDATED",
      "status": "CERTIFIED"
    }
auth:
  type: noauth
examples: ./.resources/4. Certify Provider App.resources/examples
order: 4000
