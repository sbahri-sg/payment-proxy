$kind: http-example
request:
  url: "{{base_url}}/api/v1/payment-options?environment={{execution_mode}}"
  method: GET
  headers:
    X-Emisell-Merchant-ID: "{{merchant_id}}"
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "data": [
          {
            "id": "pmo_01k3...",
            "environment": "sandbox",
            "payment_method_code": "qris",
            "category": "QR_CODE",
            "label": "QRIS"
          }
        ]
      }
order: 1000
