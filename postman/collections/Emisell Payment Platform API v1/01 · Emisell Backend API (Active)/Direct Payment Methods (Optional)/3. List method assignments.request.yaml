$kind: http-request
description: Daftar konfigurasi metode pembayaran merchant, termasuk assignment yang sedang inactive.
url: "{{base_url}}/api/v1/payment-method-assignments?environment={{execution_mode}}"
method: GET
headers:
  X-Emisell-Merchant-ID: "{{merchant_id}}"
examples: ./.resources/3. List method assignments.resources/examples
order: 3000
