# Legacy Provider App bundle

Contoh ini dipertahankan hanya untuk menguji kompatibilitas format lama
`manifest.json + connector + checksums.txt`. Submission baru harus mengikuti
`provider_submission_v1` dengan `emisell-extension.yaml` dan `openapi.yaml`
seperti `provider-apps/xendit`; native runtime binary tidak lagi dimasukkan ke
ZIP review.
